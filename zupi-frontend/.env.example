# URL do Spring Boot em desenvolvimento (proxy do Vite)
VITE_BACKEND_URL=http://localhost:8080

# Opcional: URL absoluta da API (produção ou quando não usar proxy).
# Se vazio, as requisições usam o mesmo origin do site (ex.: proxy em dev).
# VITE_API_PUBLIC_URL=https://api.seudominio.com
